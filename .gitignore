# — Python virtual environments —
.venv/
venv/
ENV/
env/

# — Byte‑compiled / optimized / DLL files —
__pycache__/
*.py[cod]
*$py.class

# — C extensions —
*.so

# — Distribution / packaging build artifacts —
build/
dist/
*.egg-info/
.eggs/

# — Installer logs and caches —
pip-log.txt
pip-delete-this-directory.txt

# — Unit test / coverage reports —
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
.pytest_cache/

# — Jupyter Notebook checkpoints —
.ipynb_checkpoints/

# — Environment variables files —
.env
*.env

# — Logs and runtime files —
*.log
logs/
app/logs/
*/logs/

# — Editor / IDE settings —
.vscode/
.idea/
*.sublime-*
*.swp
*.DS_Store

# — Operating system files —
Thumbs.db
Desktop.ini

# — Static/uploads/exports (if you generate media at runtime) —
app/static/uploads/
app/transcripts/
app/exports/

# — Docker artifacts —
*.log
docker-compose.override.yml
.env.*.local

# — Miscellaneous —
*.tmp
*.bak
