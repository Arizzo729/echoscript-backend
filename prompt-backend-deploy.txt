Goal: Make the EchoScript FastAPI backend build & run cleanly (Windows + Railway) with zero startup ImportErrors and green /api/healthz.

HARD REQUIREMENTS
- Remove any dependency on lxml unless absolutely mandatory; prefer pure-Python alternatives.
- Replace psycopg2 / psycopg2-binary with psycopg[binary]>=3.1,<4
- requirements.txt must include at least:
  fastapi>=0.115
  uvicorn[standard]>=0.30
  SQLAlchemy>=2.0
  alembic>=1.13
  pydantic>=2.7
  pydantic-settings>=2.4
  redis>=5.0
  httpx>=0.27
  passlib[bcrypt]>=1.7
  python-jose[cryptography]>=3.3
  python-dotenv>=1.0
  email-validator>=2.1
  loguru>=0.7
  psycopg[binary]>=3.1,<4
- DATABASE_URL MUST use postgresql+psycopg://…
- No route should ImportError at module import time. Do lazy imports for DB/Redis inside functions.
- /api/healthz must be DB-agnostic. /api/healthz/db and /api/healthz/redis return graceful JSON when unavailable.
- Routers mounted at /api and /v1; compat endpoints mounted without prefix (as your logs showed).
- Provide .env.example with DATABASE_URL (postgresql+psycopg://...), REDIS_URL, JWT_SECRET, CORS_ALLOW_ORIGINS, etc.
- Alembic configured for SQLAlchemy 2.x and alembic upgrade head works when DATABASE_URL is valid.

FILES TO CREATE/OVERWRITE (full contents, not diffs):
- requirements.txt (clean; no lxml; no psycopg2*)
- app/main.py, app/config.py, app/db.py, app/models.py, app/schemas.py
- app/routes/health.py, auth.py, newsletter.py, history.py, paypal.py, paypal_health.py, compat.py
- middleware/cors.py
- alembic/ (env.py + initial migration)
- .env.example
- Dockerfile (python:3.12-slim; uvicorn app.main:app --host 0.0.0.0 --port 8080)
- README.md (run instructions)

ACCEPTANCE (must pass locally):
- pip install -r requirements.txt succeeds (no lxml / psycopg2 build).
- uvicorn app.main:app --host 127.0.0.1 --port 8080 starts with NO Skipping  psycopg.
- GET /api/healthz  200 {"ok": true}
- GET /api/healthz/db  200 when DB reachable; otherwise graceful JSON.
- GET /api/healthz/redis  200 when Redis reachable; otherwise graceful JSON.
- signuploginme works; newsletter subscribe idempotent; history secured.
- Duplicate mounts at /v1; compat endpoints work without prefix.

OUTPUT FORMAT
For every changed/created file, output FULL file content in fenced code blocks named with # filename.
After files, output a Runbook with exact local run + Railway deploy steps.
